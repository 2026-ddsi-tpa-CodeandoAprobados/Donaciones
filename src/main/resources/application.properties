# PostgreSQL Database Configuration
# Using the connection string provided by Render (converted to JDBC).
# NOTE: This file now contains credentials. Don't commit to a public repo.

spring.datasource.url=jdbc:postgresql://dpg-d8k1266k1jcs73e9luk0-a.oregon-postgres.render.com:5432/donaciones_km4h?sslmode=require
spring.datasource.username=famoji
spring.datasource.password=rjpeUeg1G7yKPO2qbkll8ROGxn0fAUmC
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Actuator Configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# DevTools Configuration
spring.devtools.restart.enabled=true

# Server Configuration for Dockerfile
server.port=${PORT:8080}
